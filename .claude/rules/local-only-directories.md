# Local-only directories

The paths in the table below are gitignored and never committed, even if a branch already carried them. `local-only-paths.json` is the single source of truth: it generates this table, the `.gitignore` managed block, the pre-commit hook, and the CI check.

- A `.gitignore` rule does nothing for a file git already tracks. Untrack with `git rm -r --cached <dir>`; that keeps the files on disk.
- Add a path by editing `local-only-paths.json` and running `npm run local-only:write`. Never hand-edit `.gitignore` or this file.
- Never untrack a path because it looks like tooling. Check `main`, CI workflows, `package.json`, and imports first; the `loadBearing` list records paths that failed that test.

<!-- BEGIN generated from local-only-paths.json -->

| Path | Why it is local-only |
| --- | --- |
| `.agents/` | Agent skill definitions and lesson notes. Referenced only as an exclusion in .oxlintrc.json and .oxfmtrc.json, never imported, never invoked by CI or package.json. |
| `.codex/skills/` | Local Codex skill definitions. |
| `.cursor/` | Local Cursor hooks and skills. |
| `.design-sync/` | Machine-local Claude Design pipeline source, configuration, component metadata, previews, and build output. |
| `.ds-sync/` | Machine-local toolchain that builds, validates, and captures the Claude Design bundle. |
| `.impeccable/surfaces/` | Generated surface inventory. |
| `.logs/` | Dev-server output for the local QA harness. npm run dev:mission writes here. |
| `.product-design-audit/` | Local QA evidence: parity screenshots and screen recordings. 105 MiB of MP4 and PNG on portal/home-filter-bar as of 2026-09-05. |
| `.vercel/` | Vercel CLI link state. |
| `backend-memos/` | Local backend coordination memos. |
| `ds-bundle/` | Generated Claude Design bundle for review and sync; rebuilt from the local pipeline and repository components. |
| `plans/` | Local planning notes. |
| `screenshots/` | Ad-hoc screenshot scratch directory. Present untracked in the working tree on 2026-09-05; ignored before it can be committed. |
| `MEMORY.md` | Agent memory index. The copy on origin/main is a one-line stub. |
| `docs/` | Local briefs, screenshots and generated component inventories. The owner deleted this tree deliberately on 2026-09-05 and does not want it restored; the architecture decision records it used to hold remain in git history and are recoverable with `git show origin/main~1:docs/adr/0001-...`. |

**Not local-only.** These look like tooling but are required; the guard refuses
to run if the list above ever overlaps one of them.

| Path | Why it must stay tracked |
| --- | --- |
| `scripts/` | Tracked on main and required by CI. .github/workflows/supabase-dependency-integration.yml runs node scripts/seed-portal.mjs at line 197 and node scripts/verify-schema.mjs at line 200. package.json scripts ui:reference, dev and test:unit also invoke files here. _Untracked by commit 2b85477d on 2026-09-02, which broke the supabase-integration job; reverted the same day by 899afbdc._ |
| `.vscode/` | Two files of shared editor configuration that wire this repo's own oxlint and oxfmt toolchain (oxc.configPath points at .oxlintrc.json). Team config, not machine-local. |
| `ui-reference/` | Regenerated by npm run ui:reference (scripts/capture-ui-reference.mjs) and excluded in .oxfmtrc.json. Tracked on every branch. Reviewable-noise, not a leak. |
| `.github/` | CI workflow definitions and the dependency-automation scripts that ci.yml tests. |
| `src/` | Application source. |
| `e2e/` | Playwright specs and the target guard that npm run test:e2e-guard executes. |
| `supabase/` | Database migrations. |
| `tools/` | The custom oxlint anti-slop plugin, tested by npm run test:unit. |
| `public/` | Static assets served by Next.js. |
| `private/` | Review-flyer PDFs and QR codes verified by scripts/verify-review-flyers.mjs. |
| `.claude/` | Shared Claude Code launch configuration, repository rules, and skills symlink are tracked by owner instruction. |

<!-- END generated from local-only-paths.json -->
