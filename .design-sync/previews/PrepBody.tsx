/* Printable procedure-prep instructions, rendered from the practice's real
   prep document. */
import { PrepBody, previewPrepSections } from "westchase-gi";
export function Default() {
  return <PrepBody sections={previewPrepSections} />;
}
