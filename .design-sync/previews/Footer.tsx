/* The site footer: both offices with full NAP, hours, and the standing
   contact routes. Real EN copy. */
import { Footer, previewDict, previewLocale } from "westchase-gi";
export function Default() {
  return <Footer locale={previewLocale} dict={previewDict} />;
}
