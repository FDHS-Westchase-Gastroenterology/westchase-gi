# PR #280 UI evidence

Disposable evidence branch for FDHS-Westchase-Gastroenterology/westchase-gi#280. Captures come
from the local Playwright harness origin (http://localhost:3100) with the fictional seed identity
and the browser-side redaction. `before/` is the merge base `824277e`; `after/` is `2e0babe`, the
last source commit of `portal/print-add-surface`. Delete this branch once the PR is closed.

`<short-sha>/` folders hold the same screenshot set at every intermediate commit of the stack, so
each decision's own effect on the screen is visible next to the merge base and the tip.
