// TEMPORARY scaffold: serves the English dictionary under /vi while the
// Vietnamese translation wave lands. Replaced by the full vi translation.
import { en, type Dictionary } from "./en";

export const vi: Dictionary = en;
